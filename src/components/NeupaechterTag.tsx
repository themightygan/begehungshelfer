// Einheitliches „Neupächter"-Tag (Eintritt < 18 Monate) — siehe lib/paechter.ts.
export function NeupaechterTag() {
  return (
    <span className="rounded bg-sky-100 px-1.5 py-0.5 text-xs font-medium text-sky-800">
      Neupächter
    </span>
  );
}
