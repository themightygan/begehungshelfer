DATABASE_URL="file:./dev.db"
# Gemeinsames App-Passwort (ein Login für den Vorstand). Beliebig stark wählen.
APP_PASSWORD="CHANGE-ME"
# SESSION_SECRET = $(openssl rand -base64 32)
SESSION_SECRET="CHANGE-ME-32-bytes-base64"
STORAGE_DIR="./storage"
