# CLAUDE.md — Gartenbegehungs-App

> Lebendige Projekt-Doku. Wird jede Session automatisch geladen. **Aktuell halten.**

## Projekt
Webapp für Gartenbegehungen des **Gartenfreunde Stuttgart Sillenbuch e.V.** (Sascha ist im Vorstand).
Der Vorstand begeht mehrmals/Jahr die **>150 Parzellen** in zwei Anlagen — **Silberwald (Kürzel S)** und **Kühwasen (Kürzel K)** — und prüft Einhaltung von **Unterpachtvertrag (UPV)**, **Gartenordnung (GO)**, ggf. **Ortsrecht** (Bau-/Nachbarrecht, Abstandsflächen) und **BKleingG**.
Ziel: Papier/Doppelarbeit ersetzen; **Bericht pro Parzelle** für Akte + Pächter; Hinweise/Abmahnungen/Kündigungen mit minimalem Aufwand.

## Status (Stand 2026-05-31)
- **Was + Wie: freigegeben/entschieden.** Finaler Plan: `~/.claude/plans/ich-m-chte-mit-dir-clever-piglet.md`. Hostile Audit durchgeführt, Befunde eingearbeitet.
- **Repo:** https://github.com/themightygan/begehungshelfer (privat). Push-Auth = Account `themightygan` (NICHT MAIQ-Account `sascha-maiq` — `unset GH_TOKEN` + `gh auth login`).
- **Daten vorverarbeitet:** `data/parzellen.csv` (153 Parzellen: K=72, S=81; MK ignoriert) via `scripts/preprocess_parzellen.py`.
- **DB-Basis poliert:** SQLite-Schema + Seed stehen (`prisma/schema.prisma`, `prisma/seed.mjs`). Audit-Politur eingearbeitet (s. u.). Migration/Seed noch NICHT ausgeführt (passiert auf Mac Mini).
- **Transfer:** `SETUP_MACMINI.md` = Schritt-für-Schritt-Anleitung. Dropbox-Verschmutzung (node_modules/.next/lock) entfernt.
- **NÄCHSTER SCHRITT:** Phase 6 (Produktiv): Cloudflare permanent auf app.begehungshelfer.de (Domain-Umzug zu Cloudflare läuft, Stand 2026-05-31) + Access, always-on (launchd), Backup. Phasen 1–5 ✅. **Runden-Verwaltung jetzt echt** (Begehung starten: Anlage + Teilnehmer → /begehung mit Karte + Parzellen-Raster → Speichern&weiter → Abschluss/einfrieren → Berichte). Offen: Vorjahres-Vergleich beim Erfassen.
- **Stufe-1-Härtung Offline/Multi-User (2026-06-11) ✅:** Queue-Poison-Fix (Netzfehler zählen nicht), kein Löschen bei unerwarteten 4xx, 120-s-Timeout, Web-Locks, „hängt"-Panel (sichern/verwerfen), Client-Downscale vor Enqueue, Einfrieren serverseitig erzwungen, 48-h-Gnadenfrist für nachgereichte Medien (410 = endgültig), `abgeschlossenAm`-Migration, WAL + busy_timeout + connection_limit=1, Beitreten-Karte gegen parallele Runden, `error.tsx`, AbschlussButton blockiert bei lokalem Puffer. Smoke-Test: `scripts/smoke-stufe1.mjs` (8/8 ✅).
- **Stufe 2: Offline-first Begehungsmodus (2026-06-11) ✅:** `/begehung` ist jetzt ein Client-Workspace (Plan-Raster + Parzellen-Erfassung in EINER Seite, Hash-Routing `#p/K12`). **Sicht = Server-Snapshot (IndexedDB) ⊕ Replay der Pending-Ops** aus der generalisierten Outbox (uploadQueue, kind "op", koalesziert). Alle Texteingaben speichern automatisch lokal (onBlur/onChange → idempotente SyncOp) und syncen im Hintergrund über `/api/sync`; Mängel/Beete haben Client-UUIDs (`uid`-Spalten, Migration + Backfill). `/api/begehung/snapshot` = Batch-Snapshot (Parzellen, Katalog, Befunde, Vorjahr, offene Mängel, Messhistorie). Acks wandern via `verarbeiteAck` in den lokalen Server-Stand (kein Flicker). Mini-Service-Worker `public/sw.js` (Shell + `_next/static` + `/api/datei` cache-first) → Offline-Reload funktioniert. Alte Route `/parzelle/[id]` → Redirect; Akte/Dokumente in die Parzellenverwaltung verschoben. Wartungsmodus: `touch /tmp/begehung-maintenance` stoppt launchd-Respawn (für Migrationen). Smoke: `scripts/smoke-stufe2.mjs` (9/9 ✅) + Stufe 1 (8/8 ✅). Offen: echter Feldtest auf iPhone/iPad (Flugmodus-Durchlauf).
- **Nacharbeiten-Paket (2026-06-11) ✅:** (1) **KI-Textkorrektur** `/begehung/korrektur/[rundeId]`: lokales Ollama (`qwen3:14b`, Fallback 7b) korrigiert NUR Diktat-/Erkennungsfehler (Few-Shot + Garten-Vokabular in `src/lib/korrektur.ts`, Guards: Zahlen identisch, keine Kürzung) — Diff-Review mit Checkbox/Edit, nichts wird ungefragt geschrieben; verlinkt im Abschluss-Block + Berichten; Transkript-Glättung nutzt dieselbe Logik. (2) **Editierbare Begehungsansicht** `/begehung/ansicht/[rundeId]/[parzelleId]` (Texte/Fristen/Beete/Komp editieren, Fotos löschen/ergänzen) — folgt aus „Historie editierbar". (3) `/maengel`: Notiz + Diktate + Fotos je Mangel, Link zur Begehung. (4) Dokument-Upload mit rückdatierbarem Datum. (5) **Bilder-Import 9.6.** (`scripts/import-begehungsbilder.mjs`): 243 HEIC aus `~/Downloads/Begehungsbilder/K*` in Runde 58 importiert (26 Mangel-, 23 Beet-, 194 Übersichts-Zuordnung via Vision-Matching). Korrekturlauf Runde 58: 13 Vorschläge, Review durch Sascha unter `/begehung/korrektur/58` offen.
- **Auswertungs-/Einstellungs-Paket (2026-07-05) ✅ (deployed, Migration angewandt, Smoke 8/8+9/9):** (1) **Stufen-Umbau:** DB-Wert `hinweis`→`mitteilung` migriert (Migration `stufen_status_vorstand`), neue Stufe `gespraech` („Mündliches Gespräch" 💬) zwischen ok und mitteilung; `normalisiereStufe()` härtet `/api/sync`, `aktualisiereBefund` und Workspace-Select gegen Altclients; `STUFE_LABEL/SYMBOL` mit Legacy-Anzeige-Eintrag `hinweis`. (2) **`Befund.status`** = Nachbearbeitungs-Status (offen | muendlich | hinweis_versendet | abmahnung_versendet | abmahnung_bv | kuendigung_paechter | kuendigung_bv, `BEFUND_STATUS` in constants), Select oben rechts in der Befund-Karte der Ansicht; NICHT im Offline-Sync. (3) **Auswertung:** sortierbare Client-Tabelle (`src/app/auswertung/AuswertungsTabelle.tsx`, alle Spalten inkl. Beet-SOLL/IST getrennt), kombinierte Jahres-Anlagen-Ansicht `?jahr=&anlage=` (neuester Befund je Parzelle gewinnt — `neuesteBefundeJeParzelle` in `src/lib/auswertung.ts`, dort auch geteilte `hatDaten`/`summary`), Anlagen-Aggregat-Zeilen in der Jahres-Übersicht, Rücksprung an die Tabellenzeile via Anker `#p-<parzelleId>` + `von`-Param. (4) **Ansicht:** Abschnitt „Frühere Begehungen dieser Parzelle" (Kurzzeile + Sprunglink), zentrale „Frist für alle" (nur offene Mängel; Rückfrage vor Überschreiben individueller Fristen), Mangel löschen (mit Rückfrage, löscht auch Foto-Dateien via `dateiLoeschen` in storage.ts) und nachträglich hinzufügen (Katalog/Freitext + Notiz/Frist/Fotos). (5) **Berichte:** Balken = Direktlink zur editierbaren Ansicht (Aufklapp-Protokoll entfällt). CSV-Export auch für die kombinierte Ansicht (`/api/export/csv?jahr=&anlage=`, gleiche Merge-Regel, Dateiname `begehung_export_<Anlage>_<Jahr>.csv`). (6) **`/einstellungen`** (⚙️-Zahnrad im Header): Tab Parzellen (Anlage anlegen/umbenennen — Kürzel unveränderlich; Parzelle anlegen, startet `nicht_verpachtet`) + Tab Vorstand (Namen korrigiert: **Tomasz Weidler** statt „Günter Lorenz", **Adrian Jörreßen** statt „Jörissen" — auch in historischen `teilnehmende`-Strings; Logins mit E-Mail + scrypt-Passwort). Start-Teilnehmerliste aus DB, `TEILNEHMER`-Konstante entfernt.
- **UX-/Betriebs-Paket (2026-07-03…05) ✅:** (1) **Zwei-Benutzer-Login:** `APP_USERS` in .env (`email:passwort;…`, timingSafeEqual) — mail@sascha-theissen.de + vorstand@gartenfreunde-sillenbuch.de; Login-Formular mit E-Mail; Session speichert `benutzer`. (2) **`NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` in .env gepinnt** — sonst brechen offene Tabs nach jedem Deployment mit „Failed to find Server Action" (war Ursache des „Upload geht nicht"-Reports); error.tsx empfiehlt zuerst „Seite neu laden". (3) **Workspace-Navigation** via pushState + direktem State-Update (hashchange auf leeren Hash feuerte nicht → „zurück zum Plan"/„Fertig" wirkten tot). (4) **Auto-Save überall in der Nachbetrachtung** (`AutoSaveForm`: Server-Action ohne Form-Submit → kein React-19-Form-Reset, keine Speichern-Knöpfe; Ausnahme „+ Beet"). (5) **PDF:** sprechende Dateinamen `<Parzelle>_<NACHNAME>_<YYYY_MM_DD>.pdf`; Fotos 2/Reihe volle Breite (257×193pt), Einbettung auf 1200px/q70 gedrosselt (~330 dpi, K38 7,8→4 MB). (6) **Klickbarkeit:** Berichte-Balken = Aufklapp-Protokoll (Fotos/Mängel inline, PDF-Knopf separat, Thumb lazy); Start-„Begehungen"-Balken → Berichte; Auswertung Übersichts- + Detailzeilen ganz klickbar (`KlickZeile`; Pächter→Verwaltung, PDF-Link bleiben). (7) **Beet-Ampel** als geteilte `BeetZelle` (Auswertung + Berichte-Balken, inkl. „kompensiert"/„nicht erfasst"); Berichte-Balken mit Stufen-Symbol. (8) **Neupächter-Tag** (<18 Monate, `lib/paechter.ts`) in Begehung/Ansicht/Liste/Auswertung. (9) Teilnehmer laufender Runde inline änderbar; Mängelliste je Parzelle mit editierbarem Beet-Block (Größen/Fotos/`+ Beet`) + Mangel-Fotos/-Texten; Foto-Drag&Drop in Begehungsansicht; Dokument-Upload rückdatierbar; Erika Strack in Runde 58 ergänzt.

- **Design-System-Paket (2026-07-05) ✅ (deployed, Smoke ✓):** (1) **Impeccable installiert** (`.claude/skills/impeccable/`, `/impeccable`-Skill ab nächster Session; Detector-Hook AKTIV — meldet Design-Anti-Patterns nach jedem UI-Edit; manuell: `npx impeccable detect src/`). (2) **PRODUCT.md + DESIGN.md** = verbindlicher Design-Kontext (North Star „Das gepflegte Beet"; Register Product; **Zwei-Grau-Regel:** Text nur stone-900/600, stone-500 nur Meta, stone-400 verboten; **Ampel-Regel:** Grün/Amber/Rot nie dekorativ, nie Farbe allein; Outdoor-streng: AA + 44px-Touch). (3) **Critique** (dual-agent, Snapshots in `.impeccable/critique/`): Workspace 27/40, Auswertung 23/40, Berichte 21/40. (4) **Polish umgesetzt:** app-weiter Grau-/Kontrast-Sweep (stone-400→600/500, amber-600→800, red-600→700 bei Kleintext); `BeetZelle` trägt Wertung als Text („✓/knapp/unter Soll"); Berichte: Stufe farbig (`STUFE_TEXTFARBE` in constants) + Eskalation-absteigend sortiert + Abmahnung/Kündigung-Zeilen getönt + Status-Label statt Roh-Wert + Plural-Fix; Workspace: Stufen-Select → farbcodierte Button-Reihe, sticky Save-Status (aria-live) über der Erfassung, Diktat/Foto-✕/entfernen/pausieren/abbrechen auf 44px; Auswertung: Client-Textfilter, Sticky-Header (md+), aria-sort/scope, CSV oben, Gruppenlabels in Übersicht; `KlickZeile` respektiert Cmd/Ctrl-Klick + Tastatur; `stufeRang` normalisiert Alt-Wert `hinweis`. (5) **Semantik-Tokens** als `@theme` in `globals.css` (tinte/vereinsgruen/warn-/alarm-… = Aliase der Tailwind-Werte) — neuer UI-Code nutzt die Token-Namen. Bewusst NICHT geändert: „Fertig"→Plan-Flow (Sascha: Reihenfolge nicht linear), Emojis (Ablösung durch Icon-Set = eigenes Paket).

- **Icon-Paket (2026-07-05) ✅ (deployed, Smoke ✓):** alle UI-Emojis (~35 Stellen, 20 Dateien) durch **Lucide** (`lucide-react`) ersetzt — ein Strich-Stil, lokal gebündelt, Bundle unverändert (Tree-Shaking). Konvention in DESIGN.md §5 „Icons": `h-4 w-4` Standard, `inline-flex gap-1.5` + `aria-hidden` (Text daneben) bzw. `aria-label` wenn allein. **Stufen-Symbole (`STUFE_SYMBOL`) bleiben Emojis** (semantische Ampel, PRODUCT.md-Ausnahme). Nebenbei: Plural-Fix „n Mangel/Foto" auch in der Parzellen-Akte.

- **Katalog-/Einstellungs-Paket (2026-07-05) ✅ (deployed, Migration `verein` angewandt, Smoke 8/8+9/9):** (1) **Flächen-Abgleich** `scripts/abgleich-flaechen.mjs` (Dry-Run/`--fix`, exceljs, Header-basiert): DB stimmt 153/153 exakt mit `_quelldaten/PArzellenfläche.xlsx` überein (0 Diffs). (2) **Katalog jetzt 31 Punkte** (`scripts/katalog-update-2026-07.mjs`, idempotent; seed.mjs angeglichen): „Gemüseanbaufläche"→„Gemüseanbau: Unzureichender Anbau einjähriger Gemüsekulturen"; Sitzplatz-Grenzabstand/Anpflanzung in ZWEI Punkte getrennt; NEU „Mangelnde Zaunfreiheit" (Garten, GO-Ziffer fehlt noch) + „Unfallgefahr" (Sonstiges); „Laube – unerlaubter Bau / bauliche Veränderung". Bei 1:1-Umbenennungen ziehen auch die punkt-Snapshots alter Mängel mit (Sascha-Entscheidung; 75 Gemüse- + 4 Laube-Mängel aktualisiert, Freitexte unberührt). (3) **Anlage löschen** in `/einstellungen` (nur ohne Runden/Befunde/Dokumente/Archiv-Fotos; Name eintippen, Paste blockiert, Server validiert; FK RESTRICT als Race-Netz). (4) **Tab „Verein"**: Model `Verein` (Singleton id=1) — Vereinsname, 1. Vorsitzende/r, Adresse, E-Mail, BV-E-Mail, IMAP/SMTP (Host[:Port]), Benutzername, Passwort (write-only, Klartext in lokaler DB — wandert ins Backup!), Logo-Upload (`logoSpeichern` in storage.ts: nur PNG/JPEG/WebP per Magic-Bytes, KEIN SVG = Stored-XSS; getrennt vom statischen `public/img/logo.png` — Zusammenführung später). (5) **Vorstand löschen** nach Sicherheitsfrage (teilnehmende-Strings sind Text-Snapshots; Schema-Kommentar angepasst). (6) **PII-Excel aus Git-Index entfernt** (`git rm --cached`; Dateien bleiben lokal). DB-Backups vor Mutationen: `~/Backups/begehungshelfer/`.

- **E-Mail-Paket (2026-07-06) ✅ (deployed, Smoke 8/8+9/9; echter Postfach-Test offen — Passwort fehlt):** `src/lib/mail.ts` (nodemailer 9 + imapflow, in serverExternalPackages; Konfig aus Verein-Singleton, Host[:Port], Defaults 993/465, Timeouts 10s/10s/60s). **HITL-Garantie:** (1) Mitteilungen (Mail + Bericht-PDF an Pächter) NUR als IMAP-ENTWURF (\Drafts via SPECIAL-USE + Namens-Fallbacks, Flags \Draft+\Seen) — App sendet nie an Pächter; (2) Abmahnungs-Entwürfe per SMTP AUSSCHLIESSLICH an Vereinsadresse selbst oder Bezirksverbands-Adresse (beide serverseitig aus DB, Einzeladressen-Regex ohne ,;<>@:, Envelope gepinnt, CC an Verein bei BV-Versand als Nachweis, requireTLS bei ≠465, confirm() vor BV). Ergebnis-Audit adversarial bestanden (nodemailer fächert nicht auf — empirisch geprüft). PDF-Bau aus der Route nach `src/lib/bericht.ts` extrahiert (geteilt mit Mail-Actions; kein Versand ohne Befund). UI: Karte „E-Mail" in der Begehungsansicht (3 Buttons bzw. Konfig-Hinweis), „Verbindung testen" im Verein-Tab. Kein Auto-Statuswechsel (Befund.status bleibt manuell). Strato: klassisches Passwort-Login (imap.strato.de:993/smtp.strato.de:465; Tippfehler map→imap in DB korrigiert). **Passwort-Härtung (2026-07-06):** `emailPasswort` liegt AES-256-GCM-verschlüsselt in der DB (`enc:v1:…`, `src/lib/geheim.ts`); Schlüssel `MAIL_GEHEIM_SCHLUESSEL` in .env (nie sichern/ändern — sonst Passwort in den Einstellungen neu eintragen; mailKonfig meldet das sauber). DB-Backups tragen damit keinen Klartext. E2E verifiziert (verschlüsselt setzen → mailKonfig entschlüsselt → Buttons rendern; kein Leak ins HTML).

- **docx-Paket (2026-07-06) ✅ (Smoke 3/3, Plan- + Ergebnis-Audit im Sub-Agent bestanden):** Rendering-Fähigkeit für Vereins-Schreiben. `vorlagen/*.docx` = juristisch abgestimmte **docxtpl/Jinja2-Muster** (mitteilung, abmahnung_verein, abmahnung_bv — nur befüllen, nie neu bauen; Begleitdoku `~/Downloads/README_Datenvertrag.md` + Textbausteine-/Rechtssätze-md). `scripts/render_docx.py` (data/.venv, Deps gepinnt in `scripts/requirements-render.txt`): Job-JSON → docx; **Pillow-Reencode aller Bilder Pflicht** (sharp strippt JFIF → python-docx `UnrecognizedImageError` bei 100 % der App-Fotos; Fotos 900px/55mm, Logo 48mm, exif_transpose), autoescape (kein XML aus Nutzertext), StrictUndefined + Pflichtfeld-Vorabprüfung (exit 3 + Liste), leere Beanstandungsliste abgelehnt. `src/lib/docx.ts` = Wrapper (transkript-Muster, absolute venv-Pfade). docx→PDF via `/opt/homebrew/bin/soffice` (absoluter Pfad; bei Parallelität eigenes `-env:UserInstallation`-Profil). Smoke: `scripts/smoke-docx.mjs`. Gotchas: identische Bilder dedupliziert docx (`<a:blip>`-Platzierungen zählen, nicht Media-Dateien); `historie_hinweise` ohne Verb liefern (Vorlage hängt „angeschrieben" an); SOLL/IST-Gemüse hat KEINE eigenen Platzhalter — gehört in `beanstandungen[].text`; `empathie_grund` in keiner Vorlage vorhanden. **Teil 2 (Schreiben-Generator aus Begehungsdaten) — Vorarbeiten ✅ (2026-07-06, deployed):** Migration `anrede_briefdaten`: `Parzelle.anrede` (herr|frau|""=nachfragen) + `anredeStil` (sie|du; Abmahnungen immer „Sie"), Verein: `telefon/ort/bvName/bvStrasse/bvPlzOrt`; Pflege in Parzellen-Stammdaten/Verein-Tab, Pächterwechsel resettet Anrede. `scripts/anrede-vorbelegen.mjs`: 144/151 gesetzt, **7 Zweifelsfälle offen** (K11 Enkhsaruul, K66 Dana, K9 Dores, S23 Narinder, S43 Jasmin, S46 Kay, S57 Hilal). **Leere-Mängel-Analyse:** Gemüse 32/38 deterministisch aus Beet-IST+SOLL (6 ohne Beete → „nicht bestellt"-Bestätigung im Dialog; Plausi: IST>=SOLL abfangen, s. K55); Freitext-`punkt` IST der Befund; echte Lücke nur ~16 Pflicht-Befund-Mängel in Abmahnungen (12× Pflegezustand, 3× Müll, 1× Staudenrabatten) + 4 ganz leere Freitexte → Nachtrag im Erzeugen-Dialog. **Ollama-Benchmark** (55 echte Freitexte): qwen3:14b GEWÄHLT (55/55 JSON, Ø4,1s, konservativ, „KEIN" sinnvoll) vs 7b (schneller, aber Fehlzuordnungen + ID-Format unsauber); Lernpunkt: Baustein-Liste braucht generisches „Unfallgefahr" (Katalog 31). Entschieden: Bausteine als TS-Konstante (kein Editor/DB), Schreiben-Fluss = docx erzeugen → als Mail-Entwurf → in Word finalisieren → versandtes Schreiben in die Akte (Upload existiert in Parzellen-Akte), §12-Weiche via `Parzelle.eintritt` >= 2024-05-01. Erzeugen-Dialog in der Begehungsansicht = nächstes Paket. **Generator-Kern ✅ (2026-07-06):** `src/lib/bausteine.ts` (36 Bausteine als TS-Konstante inkl. NEU S09 Unfallgefahr + KATALOG_ZU_BAUSTEIN) + `src/lib/schreiben.ts` (pure, ohne DB: SOLL/IST-Gemüse mit §12/§1-Weiche, Fristen ab **max(Begehung, heute)** inkl. BNatSchG/30.10., Brieffrist=späteste Standardfrist + Einzelfristen, Betreff deterministisch via `aufzaehlung()`, Warnungen). Testlauf `scripts/schreiben-test.mjs` (kompiliert Libs via tsc nach /tmp, LLM nur Freitext→Baustein): K51-Mitteilung/K8-Abmahnung-1/K53-Abmahnung-2 gerendert + visuell geprüft (auf ~/Desktop/Schreiben-Entwuerfe). Alle 7 Anrede-Zweifelsfälle von Sascha geklärt (DB), Verein-Briefkopf+BV-Adresse in DB befüllt. Entscheidung: Entwürfe nehmen vorhandene Befund-/Freitexte, Feinschliff in Word/Mail (kein Nachtrags-Zwang bei Mitteilungen); K53-Historie wird aus Dokumenten-Akte vorgeschlagen. **Feinschliff-Paket (2026-07-06 abends) ✅ (deployed, Smoke 8/8+9/9):** Vorlagen chirurgisch angepasst (Kontakt-/Unterschrifts-NAMEN raus — nur Verein/BV + handschriftliche Signatur; Abmahnsatz FETT; „bis zum {{ frist_datum }}" UNTERSTRICHEN; „Bestandteil dieser Abmahnung"- und „Anlagen:"-Passus entfernt). Bausteine: Feststellungen jetzt {befund}-String-Templates (`feststellungBauen`), NEU **S10/S11 Bienenhaltung** (GO 8.2 — ohne Genehmigung/derzeit nicht genehmigungsfähig bzw. zu viele Völker). **Fristen-Modell (Sascha):** EINE zentrale Brieffrist = späteste REGULÄRE Tages-Frist; Saisonziele (30.10./BNatSchG) + Gefahr-Eilfristen als Einzelfrist je Position; Gemüse-Abmahnung bis Ende Juli = Beet-Anlage noch dieses Jahr (mind. 28 Tage, späte Kulturen/Phacelia), sonst/Mitteilung 30.04. Folgejahr. **Tab `/einstellungen?tab=textbausteine`**: Feststellung/Aufforderung/Norm je Baustein editierbar (Model `TextbausteinOverride`, Migration `textbaustein_override`; leer/Default = Code gilt, Zurücksetzen löscht; G01-Gemüsetexte bewusst nicht editierbar). **Schreiben-auf-Knopfdruck ✅ (2026-07-06, deployed, Auth-Curl 4 Seiten ✓):** `src/lib/schreibenErzeugen.ts` = voller Prozess je Parzelle (Typ aus Befund-Stufe): Mitteilung → docx→PDF→IMAP-ENTWURF an Pächter; Abm. 1 → docx an VEREIN; Abm. 2 → docx an BV („Bitte um Übernahme", CC Verein). Karte „Schreiben erstellen" in der Begehungsansicht (Anrede-Nachtrag inline, Historie-Felder mit Akten-Vorschlag, Warnungen als Liste). **Sammel-Erstellung in der Auswertung** (Runden- + Jahres-Ansicht): Checkbox-Spalte (nur Schreiben-Stufen), sequenzieller Client-Lauf über `sammelSchreiben`-Action (kein Batch-Timeout; confirm mit Typ-Aufschlüsselung + BV-Warnung; ✓/⚠/✗ je Zeile). `gesendetAbgleich()` in mail.ts + Button im Verein-Tab: legt versandte Mitteilungen (Betreff „Parzelle X", PDF-Anhang, Dedupe via Message-ID in Dokument-Notiz) automatisch in die Akte. OFFEN: echter Postfach-Durchlauf (Passwort-Status prüfen), launchd-Cron für täglichen Abgleich (Phase 2). **Feinschliff II (2026-07-06 spät) ✅ (deployed):** Abm. 2 = IMAP-ENTWURF an BV („Bitte um Abmahnung durch den BV", docx anbei) statt SMTP-Direktversand; Mitteilungs-Entwurf mit PDF+docx (`entwurfInPostfach` nimmt `anhaenge[]`); Mail-Gruß „Der Vorstand"; `nameSchoen()`/`vornameTitel()` in schreiben.ts (CAPS-Nachnamen normalisiert, Dr./Prof. aus Vorname→förmliche Anrede, Du-Form = Rufname — KEINE Datenänderung nötig); Mitteilungs-Vorlage mit bedingten Absätzen `plakette` (+`plakette_jahr`, `hinweis_anzahl_text` Sg./Pl.) und `neupaechter_lob` (Neupächter && (Plakette||plakettenNotiz), nie doppelt mit Plakette); Ansicht: Typ folgt Befund-Stufe (kein Select), alte Bericht-Mail-Karte/MailAktionen/Actions ENTFERNT — „Schreiben erstellen" ersetzt sie. Mitteilung = **Sandwich** (Einleitung → Plakette-/Lob-Absatz → UPV-Erläuterung → Frist); BV-Entwurf legt die 1. Abmahnung aus der Akte (typ schreiben, „Abmahnung" in Notiz) zur Kenntnis bei. Test-Referenzen: Neupächter+Plakette K35/Runde 96, Neupächter ohne S18/97, Abm.1 K8/96, Abm.2 K53/96 (mit Akten-Beilage 2023). Nachschliff: Nur-Tag-Absätze (`{% if %}` allein) in allen 3 Vorlagen auf `{%p %}` umgestellt (hinterließen Leerzeilen = ungleiche Abstände); G07 Obstbaumschnitt in MITTEILUNGEN = Frist stets 31.03. (Winterschnitt), Abmahnungen 30.10. Entwürfe landen in eigenen, beim Anlegen ABONNIERTEN IMAP-Ordnern ('Entwürfe Mitteilungen' / 'Entwürfe 2. Abmahnungen', Namespace-Präfix vom Drafts-Pfad) — fehlendes SUBSCRIBE war die mutmaßliche Ursache für unsichtbare Entwürfe auf Zweitgeräten. **Nachbegehung** hebt Parzellen mit ausgesprochener Abmahnung/Kündigung hervor: Snapshot-Feld `eskalation` (neuester Alt-Befund mit Stufe abmahnung_1/2/kuendigung + Mangel-Fristen; optional wg. alter IndexedDB-Caches), Raster-Kachel mit rotem 2px-Rahmen + Badge '1. Abm./2. Abm./Künd. · Frist', roter Hinweis-Block in der Parzellen-Erfassung. Ordner-Lektion: '.' ist bei Strato IMAP-Delimiter -> Entwurfs-Ordner sind punktfreie UNTERORDNER von Entwürfe ('Mitteilungen', 'Abmahnungen BV'). Offene Mängel in der Nachverfolgung: aufklappbar mit Alt-Fotos + Beleg-Foto-Aufnahme (offeneFruehere[].fotos im Snapshot, verarbeiteAck sortiert Acks dorthin; /api/foto hängt Beleg via mangelUid an den ALTEN Mangel). Doppelversand-Schutz: Akte-Dokument (schreiben/email) mit Datum >= Begehungstag = 'Schreiben erledigt' -> Auswertung ✓✉ statt Checkbox, Einzel-Karte zeigt Banner (bewusstes Erneut-Erstellen bleibt möglich). Zoom-Lektion: CSS-zoom auf <body> rendert auf iOS-Safari überlappend -> ZoomControl skaliert html.fontSize (rem-Basis).

### Audit-Entscheidungen (gesetzt)
- **Offline = Formular-Puffer:** online-first, aber offener Befund (Eingaben + Fotos) lokal im Browser puffern, Auto-Retry. Voll-PWA = Phase 2. (Stufe 2 ✅ — voll offline.)
- **Historie = NICHT mehr eingefroren (geändert 2026-06-11):** Abschluss beendet die aktive Erfassung + erzeugt Berichte, aber Texte bleiben korrigierbar / Fotos löschbar (editierbare Begehungsansicht). „behoben" weiter via `Mangel.behobenAm`; Befund-Snapshot inkl. Adresse bleibt.
- **Auth = Cloudflare Access (Pflicht)** zusätzlich zum gemeinsamen App-Passwort. Passwort-Vergleich `crypto.timingSafeEqual` (kein bcrypt).
- **Foto-Pipeline (MUSS beim Upload):** resize ~1600px + JPEG-Q~75 + **EXIF/Geo-Strip** (DSGVO). Treiber: bis 24 Fotos/Parzelle.
- **Schema-Politur:** Plakette in Befund-Felder aufgelöst; `soll_obst` entfernt; zentrale Wertquelle `src/lib/constants.ts` (beim Bau anlegen); Eskalationsstufe bleibt pro Befund.

## Leitprinzip
**KISS / Pareto** — 80 % Nutzen, 20 % Aufwand. Jedes Feature so schlicht wie möglich; Erweiterungen klar als **Phase 2** markieren. Scope-Creep aktiv ansprechen und einfachere Alternativen vorschlagen.

## Architektur (entschieden)
- **App:** Next.js (TypeScript, App Router) + Tailwind. Mobile-first (Handy/iPad) + Desktop.
- **DB:** SQLite (Datei `prisma/dev.db`, kein DB-Server) via Prisma 6. KISS für single-host; Backup = Datei kopieren. SQLite hat keine nativen Enums → Auswahlfelder als String mit dokumentierten Werten.
- **Datei-Speicher:** lokales Dateisystem (Fotos + PDFs). Backup → Dropbox/Time Machine.
- **Auth:** `Vorstand`-Modell in der DB (E-Mail + scrypt-Hash/Salt via `src/lib/passwort.ts`, Pflege in `/einstellungen` → Tab Vorstand) hat Vorrang; `APP_USERS` (.env) bleibt Fallback/Sicherheitsnetz gegen Aussperren. Kein Auth-Framework. E-Mails lowercased; hat eine Adresse ein DB-Passwort, gilt NUR dieses.
- **Hosting:** SELF-HOSTED auf Sascha's **Mac Mini M4 (16 GB)**, Glasfaser. Kein Cloud-Hoster.
- **Fernzugriff:** **Cloudflare Tunnel** (`cloudflared`) — kein DynDNS/Port-Forwarding, kostenloses TLS, funktioniert hinter CGNAT/DS-Lite. Optional **Cloudflare Access** als Zugriffsschutz.
- **Lokale KI (Phase 2):** Transkription `whisper.cpp`/`faster-whisper`; Textglättung + Brief-Entwürfe via lokales **Ollama**.
- **Datensouveränität:** alle personenbezogenen Daten bleiben lokal. Keine Cloud, kein US-Hoster.

> **Ersetzt zwei Altsysteme** (kein Code-Reuse, KEINE Datenmigration — frischer Start):
> - `gartenfreunde` (Vue3/FastAPI/Supabase) — lief produktiv (~150 Parzellen, Begehungen 2025).
> - `begehungshelfer` (geplant: Next.js + Supabase + Dropbox + OpenAI KI-Briefe) — nur Planung, nie gebaut.
> Beide unter `…/Coding/Gartenfreunde_old/`. Wir lernen vom Schema, bauen aber frisch & schlank.

## Daten
- **Parzellen-Schlüssel:** `Kürzel + Nummer + Index` → z. B. „K1", „S59a".
- **Stammdaten-Quellen (vor Import vorzuverarbeiten):**
  - Mitglieder: `/Users/saschatheissen/Downloads/April 2026 Mitgliederliste.xlsx` (Sheet „Gartenverz. & Mitgliederstatus") — Pächter, Kontakt, Adresse, Eintritt. Hunderte Wasser-Spalten ignorieren.
  - Flächen: `…/Gartenfreunde_old/Daten/Echte Daten/PArzellenfläche.xlsx` (Sheet „JR 2024-2025", `parzellengroesse` m²).
  - **Join:** `Anl. + Ga-Nr + Ind.`. Excel-Datumsserien + Formelzellen auflösen.
- **Mängelkatalog:** aus `…/Gartenfreunde_old/Daten/Muster/Formular_Gartenbegehung_neutral.pdf` (3 Bereiche, ~28 Punkte — Details in der Spec).
- **Pläne:** Silberwald + Kühwasen vorhanden (`_quelldaten/plaene/`). Anlagenliste final = nur K + S. Farbige Markierungen auf den Plänen (grün/rot/blau) sind Notizen aus anderem Kontext → **ignorieren**, Plan dient nur als statisches Orientierungsbild.
- **Regelwerk:** `…/Muster/Gartenordnung.txt`, `…/Muster/Unterpacht-Vertrag.txt`. Quote: **UPV §12 = min. 1/6 Gemüse + 1/6 Obst** (GO strebt 1/3 gesamt an).

## Bau-Reihenfolge (Phasen — jede lauffähig & verifizierbar)
0. **Fundament — ✅ FERTIG:** SQLite migriert + geseedet (153 Parzellen, 2 Anlagen, 28 Katalogpunkte).
1. **De-Risk-Prototyp — ✅ FERTIG:** Next.js 15 (App Router, TS, Tailwind v4) gescaffoldet + Foto/PDF-Durchstich verifiziert. `sharp`-Pipeline (HEIC/iPhone→JPEG via `heic-convert`, resize ~1600px, JPEG q75, EXIF/Geo-Strip nachgewiesen), Befund-Erfassung (Stufe/Notiz/Fotos), Bericht-PDF via `@react-pdf/renderer`. `src/lib/constants.ts`, `db.ts`, `storage.ts`, `pdf.tsx`.
2. **Login — ✅ FERTIG:** gemeinsames Passwort via `crypto.timingSafeEqual` + iron-session (`begehung_session`-Cookie); `src/middleware.ts` schützt alle Routen außer `/login` + Assets; Abmelden im Header.
3. **Erfassung — ✅ FERTIG:** Mängel-Menü (28 Katalogpunkte als Chips, nach Bereich gruppiert) → Mangel hinzufügen, pro Mangel Karte mit Maßnahmentext + Frist + mehrere Fotos; Gesamtansicht-Fotos (Orientierung, im PDF vorne); Befund-Stufe + Bemerkung (am Seitenende) + „Speichern & weiter" zur nächsten Parzelle; Vor/Zurück (anlagenintern); Freitext-Mangel; **Gemüsebeete (bis 5, IST vs. SOLL 1/6, UPV §12)**. PDF gruppiert Fotos je Mangel + Gemüse-Soll/Ist. **Echte Runden-Verwaltung** (Begehung starten mit Anlage+Teilnehmern, aktive Runde in Session, Karte/Plan-Bild der Anlage, Abschluss friert ein). Plan-Bild liegt unter `storage/plaene/`, `Anlage.planBild` in DB (nicht im Repo; nur Kühwasen vorhanden).
4. **Nachverfolgung — ✅ FERTIG (ohne Vorjahr):** `/maengel` listet offene/überfällige Mängel (überfällig zuerst), Filter offen/alle, „behoben"-Toggle via `behobenAm`. **Offen:** Vorjahres-Vergleich beim Erfassen (braucht Runden-Verwaltung).
5. **Dokumente & Export — ✅ FERTIG:** Akte je Parzelle (Dokument-Upload: Schreiben/E-Mail/Wertermittlung/sonstiges, beliebige Dateitypen); PDF pro Parzelle; CSV-Gesamtexport (`/api/export/csv`, UTF-8-BOM + Semikolon für Excel/DE).
6. **Produktiv:** Mac Mini always-on (launchd/pm2), Cloudflare Tunnel + Access (begehungshelfer.de), Backup (DB + storage).
🟡 **Phase 2+:** Sprachnotiz→whisper.cpp→Ollama-Glättung; KI-Briefe; interaktiver Plan; persönliche Konten; formale Nachbegehung.

## Audit-Entscheidungen (verbindlich)
- **Offline = Formular-Puffer:** online-first; offener Befund (Eingaben+Fotos) lokal im Browser puffern + Auto-Retry. Voll-PWA = Phase 2. (Stufe 2 ✅.)
- **Historie = editierbar (geändert 2026-06-11):** kein hartes Einfrieren mehr — Texte korrigierbar, Fotos löschbar (Doku-Verständlichkeit vor Unveränderlichkeit); „behoben" nur via `Mangel.behobenAm`; Befund-Snapshot inkl. Adresse.
- **Auth = Cloudflare Access (Pflicht)** + gemeinsames App-Passwort (`crypto.timingSafeEqual`, KEIN bcrypt).
- **Foto-Pipeline (Pflicht beim Upload):** resize ~1600px + JPEG-Q~75 + **EXIF/Geo-Strip** (DSGVO). Treiber: bis 24 Fotos/Parzelle.
- **String-Auswahlfelder:** EINZIGE Wertquelle in `src/lib/constants.ts` (beim Bau anlegen) — Schema-Kommentare spiegeln sie nur.
- **Pläne:** farbige Markierungen ignorieren (nur Orientierungsbild).

> Hinweis: Der ausführliche Plan + Memory liegen auf dem MacBook (Prep-Maschine), NICHT hier. Diese CLAUDE.md ist die maßgebliche Quelle auf dem Mac Mini. Bei „live state"-Zweifeln: Schema/DB/Code direkt prüfen.

## Konventionen für Claude
- **UI/Texte: Deutsch.** Antworten an Sascha: Executive Summary zuerst (siehe globale `~/.claude/CLAUDE.md`).
- Chirurgische Änderungen, bestehenden Stil treffen, kein Drive-by-Refactoring.
- Live-Stand prüfen statt Doku/Memory blind vertrauen.
- Vor „fertig": verifizieren (App starten / Funktion prüfen), nicht nur „läuft".
- **`.next` ist geteilt:** `next dev` (egal welcher Port) zerstört den Produktions-Build → nach jedem Dev-Lauf und vor jedem App-Restart `npm run build`, sonst 502 (Vorfall 2026-07-05).
- **Design:** PRODUCT.md/DESIGN.md sind verbindlich (Zwei-Grau-Regel, Ampel-Regel, Lucide-Icons, 44px im Feld); Detector-Hook meldet Verstöße automatisch, `/impeccable` für Reviews.
- Repo `themightygan/begehungshelfer`. **NIE** `data/`, `.env`, `storage/`, `*.db` committen (PII/Secrets — siehe `.gitignore`). Commits: globale `~/.claude/rules/commit-format.md`.
- Dies ist ein Vereins-/Privatprojekt (nicht MAIQ).

## Offene Punkte
- E-Mail: Sascha trägt das Postfach-Passwort ein (Einstellungen → Verein) + klickt „Verbindung testen"; danach ein echter Entwurf-/Versand-Durchlauf. Offene Inhaltsfrage: erwartet der BV einen formalen Abmahnungs-BRIEF (= Phase-2 „KI-Briefe") oder reicht Standardtext + Bericht-PDF (aktueller Stand)?
- GO-Ziffer für Katalogpunkt „Mangelnde Zaunfreiheit" nachtragen (Gartenordnung.txt liegt nicht auf dieser Maschine; referenz-Feld leer).
- Git-Historie enthält die PII-Excel noch aus dem Initial-Commit (Index bereinigt; Bereinigung via `git filter-repo` mit Sascha entscheiden).
- Feldtest der neuen Feld-UI (Stufen-Buttons, Save-Status, 44px-Ziele) auf dem iPhone; danach `/impeccable critique`-Re-Run (Baseline: 27/23/21).
- Cloudflare **Access** einrichten (app.begehungshelfer.de läuft bereits über Named Tunnel).
- Backup-Routine (DB-Dump + Files → Dropbox) einrichten.
- KI-Korrektur-Review Runde 58 (`/begehung/korrektur/58`) durch Sascha.
- Feldtest Offline-Workspace auf iPhone/iPad (Flugmodus-Durchlauf) — Stufe 2 ist gebaut, aber noch nicht im Feld verifiziert.
